// ANTLR stuff is commented out, because we generate in IDE and commit the generated files directly
//apply plugin: 'antlr'
apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
//	antlr "org.antlr:antlr4:4.5.1"
	compile "org.antlr:antlr4:4.5.1"
	testCompile 'org.testng:testng:6.8.21'
}

//generateGrammarSource {
//	maxHeapSize = "64m"
//	arguments += ["-visitor", "-long-messages"]
//}