archivesBaseName = 'javasimon-jdbc41'

dependencies {
	compile project(':core')
	testCompile ('com.h2database:h2:1.3.170')
}


sourceCompatibility = '1.7'
targetCompatibility = '1.7'

compileJava {
    options.compilerArgs << '-bootclasspath' << getBootstrapPath("1.7")
    options.fork = true
}